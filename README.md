# next-redux-todolist

提示词 

新建一个todolist的项目，使用nextjs 作为前端框架，状态管理使用reduxtookit，完成后在本地启动，如果有报错解决报错后重新启动

- 使用agent的auto 模型，运用了next 12和react 18，可以生成
- 使用agent的sonnet 3.5模型，运用了next-create-app 和redux toolkit, 项目报错，解决错误的能力还在评估中
